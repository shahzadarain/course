browser-sync start --server --directory --files "**/*"
atom. for open atom in same diectory 
git status  to see the modified status
git add.   ;add all files . is used for all files
git commit -m "any title"  commit the modification 
git push  for upload

git config --global user.email "any email" 

git config --global user.name "any user" 